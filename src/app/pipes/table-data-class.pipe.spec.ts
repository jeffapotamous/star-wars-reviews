import { TableDataClassPipe } from './table-data-class.pipe';

describe('TableDataClassPipe', () => {
  it('create an instance', () => {
    const pipe = new TableDataClassPipe();
    expect(pipe).toBeTruthy();
  });
});
